- !policy
  id: {{ TEST_APP_PROJECT_NAME }}
  owner: !group developers
  body:
  - !layer

- !policy
  id: {{ TEST_APP_PROJECT_NAME }}-db
  owner: !group operations
  body:
  - &variables
    - !variable password

  - !permit
    resources: *variables
    privilege: [ read, execute ]
    role: !layer /test-app
